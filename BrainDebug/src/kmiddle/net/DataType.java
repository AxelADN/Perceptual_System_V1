package kmiddle.net;

import kmiddle.utils.fields.IndexConstants;

public class DataType implements IndexConstants {
	/*
	public static short getDataType(byte[] msg){
		return getDataType(I_DATA_TYPE, msg);
	}

	public static short getSecondDataType(byte[] msg) {
		return getDataType(I_SECOND_DATA_TYPE, msg);
	}
	
	private static short getDataType(int index, byte[] msg){
		ByteBuffer buffer = ByteBuffer.allocate(msg.length);
		buffer.put(msg);
		return buffer.getShort(index);
	}
	*/
}
